local options = {
  panel = {
    enabled = false,
  },

  suggestion = {
    enabled = false,
    auto_trigger = false,
  },

  filetypes = {
    yaml = true,
    markdown = true,
    gitcommit = true,
    gitrebase = true,
  },

  server = {
    type = "binary",
  },
}

return options
